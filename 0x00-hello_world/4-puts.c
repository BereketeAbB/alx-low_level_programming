/**
 * main -First C prog
 *
 * Description: puts,
 */

#include <stdio.h>
#include <string.h>
int main () {
    string ome;
    strcpy(one, \"Programming is like building a multilingual puzzle);
    puts(one);
    return (0);
}
